# NOTE: do NOT add trailing slashes to directory rules. `az acr build` compiles
# a rule like `node_modules/` to the regex `^node_modules/$`, which never matches
# tar entry names (they have no trailing slash) — so the rule becomes a silent
# no-op and the directory gets uploaded anyway. Write `node_modules`, not `node_modules/`.

# Git
.git
.gitignore
.github

# Python environments & cache
__pycache__
**/__pycache__
*.py[cod]
*.egg-info
.venv*
venv
.pytest_cache
.ruff_cache
.mypy_cache
.coverage
htmlcov

# IDE / OS
.idea
.vscode
.DS_Store

# Tests (not needed in production)
tests
e2e
test-results

# Documentation
*.md

# Logs
*.log
logs
rbaccatalog/web/logs

# Node.js (only needed for dev/testing)
node_modules
package.json
package-lock.json
config

# Config files not needed in container
pyproject.toml
.pre-commit-config.yaml

# Local database
*.db

# Azure / CI config
.azure

# Cache directories
.cache
rbaccatalog/.cache

# Local secrets
.env
.env.*

# Docker (don't copy Docker files into image)
Dockerfile
docker-compose*.yml
.dockerignore

# Build artifacts
deploy.zip
build
dist

# Large ML models (downloaded at runtime)
rbaccatalog/airecommender/models/all-MiniLM-L6-v2

# Generated version file
rbaccatalog/_version_generated.py

# Dev/CI scripts, docs, and infra (not copied into the image)
scripts
docs
infra